import XCTest
@testable import SwifttObjcPackage

final class SwifttObjcPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwifttObjcPackage().text, "Hello, World!")
    }
}
